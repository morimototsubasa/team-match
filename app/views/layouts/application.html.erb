<!DOCTYPE html>
<html>
  <head>
    <title>TeamMatch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="row">
        <h2 class="col-md-4">Team-Match</h2>
        <div class="col-md-8 btn-toolbar">
          <ul class="navbar-nav float-right">
            <% if customer_signed_in? %>
            <li><%= link_to "新規募集", new_recruit_path %></li>
            <li><%= link_to "募集一覧", recruits_path %></li>
            <li><%= link_to "マイページ", customer_path(current_customer.id) %></li>
            <li><%= link_to "ログアウト", destroy_customer_session_path, method: :delete %></li>
            <% else %>
            <li><%= link_to "募集一覧", recruits_path %></li>
            <li><%= link_to "新規会員登録", new_customer_registration_path %></li>
            <li><%= link_to "ログイン", new_customer_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
